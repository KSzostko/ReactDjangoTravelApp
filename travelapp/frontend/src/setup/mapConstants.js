const defaultCenter = {
  lat: 49.505,
  lng: 24,
};

const mapSettings = {
  zoom: 13,
  minZoom: 4,
  maxZoom: 18,
  maxClusterRadius: 100,
  maxBoundsViscosity: 0,
  spiderifyOnMaxZoom: false,
  defaultCenter,
};

export default mapSettings;
